# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MediaMagicAssessment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MediaMagicAssessment
   pod 'SDWebImage', '~>3.8'

  target 'MediaMagicAssessmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MediaMagicAssessmentUITests' do
    # Pods for testing
  end

end
